server:
  port: 8001

eureka:
  client:
    #表示该eureka节点不能注册服务
    register-with-eureka: false
    #表示该eureka节点不能发现（订阅）服务
    fetch-registry: false
  instance:
    #安全保护机制（端口）关闭
    secure-port-enabled: false